```
JS 2

function calculaPrecoTotal(quantidade) {
  // Escreva seu código aqui
 let macasCara = quantidade * 1.3
  let macasBarata = quantidade * 1
  if( quantidade >= 12){
   return macasBarata
  } else{ return  macasCara}
}

```
